{
  "name": "scripture-help",
  "version": "1.0.0",
  "private": true,
  "description": "Backend-backed PWA for scripture encouragement and NIV lookup.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check topics.js && node --check engine.js"
  }
}
